# 使用内存数据库避免垃圾数据的产生
spring:
  datasource:
    url: jdbc:h2:mem:manager;DB_CLOSE_ON_EXIT=FALSE
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
server:
  context-path: /manager
  port: 8081